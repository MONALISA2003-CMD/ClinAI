'use client';
import {useEffect,useState} from 'react';

const API=(process.env.NEXT_PUBLIC_API_URL||'https://clinai-api.onrender.com').replace(/\/$/,'');
type Row=Record<string,any>;
type Field={key:string;label:string;type?:string;placeholder?:string;options?:string[];required?:boolean};
const groups:any=[
 ['CARE',['patients','appointments','registration','queue','triage','encounters','clinical-notes','diagnoses','orders','care-plans','referrals','follow-up']],
 ['CLINICAL',['laboratory','imaging','pharmacy','nursing','emergency','inpatient','beds','surgery','maternity','pediatrics','immunization','chronic-care']],
 ['FINANCE',['billing','payments','insurance','claims','accounting']],
 ['SUPPLY',['inventory','procurement','suppliers']],
 ['PATIENT',['patient-portal','telemedicine','remote-monitoring','notifications','documents']],
 ['INTELLIGENCE',['ai','analytics','population-health']],
 ['PLATFORM',['staff','facilities','tasks','workflows','interoperability','trust','audit','settings']]
];
const journey:any=[['Registration','registration'],['Check-in & Queue','queue'],['Triage & Vitals','triage'],['Encounter','encounters'],['Orders','orders'],['Lab / Imaging','laboratory'],['Pharmacy','pharmacy'],['Billing','billing'],['Discharge','inpatient'],['Follow-up','follow-up']];
const pretty=(x:string)=>x.replaceAll('-',' ').replace(/\b\w/g,c=>c.toUpperCase());
const fields:Record<string,Field[]>= {
 patients:[{key:'firstName',label:'First name'},{key:'lastName',label:'Last name'},{key:'phone',label:'Phone'}],
 appointments:[{key:'patientId',label:'Patient ID'},{key:'startAt',label:'Start date/time',type:'datetime-local'},{key:'reason',label:'Reason'}],
 registration:[{key:'patientId',label:'Patient ID'},{key:'status',label:'Status',options:['registered','checked-in','completed']}],
 queue:[{key:'patientId',label:'Patient ID'},{key:'priority',label:'Priority',options:['normal','routine','urgent','emergency','stat']}],
 triage:[{key:'patientId',label:'Patient ID'},{key:'encounterId',label:'Encounter ID'},{key:'chiefComplaint',label:'Chief complaint'},{key:'temperature',label:'Temperature'},{key:'heartRate',label:'Heart rate'},{key:'respiratoryRate',label:'Respiratory rate'},{key:'systolic',label:'Systolic BP'},{key:'diastolic',label:'Diastolic BP'},{key:'spo2',label:'SpO₂'},{key:'pain',label:'Pain 0 to 10'},{key:'acuity',label:'Acuity',options:['routine','urgent','emergency']}],
 encounters:[{key:'patientId',label:'Patient ID'},{key:'appointmentId',label:'Appointment ID'},{key:'reason',label:'Reason'}],
 'clinical-notes':[{key:'encounterId',label:'Encounter ID'},{key:'noteType',label:'Note type',options:['SOAP','progress','discharge','nursing']},{key:'subjective',label:'Subjective'},{key:'objective',label:'Objective'},{key:'assessment',label:'Assessment'},{key:'plan',label:'Plan'}],
 diagnoses:[{key:'patientId',label:'Patient ID'},{key:'encounterId',label:'Encounter ID'},{key:'code',label:'Diagnosis code'},{key:'description',label:'Diagnosis description'}],
 orders:[{key:'patientId',label:'Patient ID'},{key:'encounterId',label:'Encounter ID'},{key:'code',label:'Order code'},{key:'description',label:'Description'},{key:'category',label:'Category',options:['laboratory','imaging','medication','procedure']},{key:'priority',label:'Priority',options:['routine','urgent','stat']}],
 laboratory:[{key:'patientId',label:'Patient ID'},{key:'code',label:'Test code'},{key:'description',label:'Test name'},{key:'priority',label:'Priority',options:['routine','urgent','stat']}],
 imaging:[{key:'patientId',label:'Patient ID'},{key:'code',label:'Study code'},{key:'description',label:'Study'},{key:'priority',label:'Priority',options:['routine','urgent','stat']}],
 pharmacy:[{key:'patientId',label:'Patient ID'},{key:'medicationCode',label:'Medication code'},{key:'medicationName',label:'Medication name'},{key:'dose',label:'Dose'},{key:'frequency',label:'Frequency'},{key:'route',label:'Route'},{key:'duration',label:'Duration'},{key:'quantity',label:'Quantity'}],
 nursing:[{key:'patientId',label:'Patient ID'},{key:'task',label:'Nursing task'},{key:'priority',label:'Priority',options:['normal','urgent','stat']}],
 emergency:[{key:'patientId',label:'Patient ID'},{key:'chiefComplaint',label:'Chief complaint'},{key:'acuity',label:'Acuity',options:['urgent','emergency']}],
 inpatient:[{key:'patientId',label:'Patient ID'},{key:'ward',label:'Ward'},{key:'bed',label:'Bed'}],
 beds:[{key:'ward',label:'Ward'},{key:'bedNumber',label:'Bed number'},{key:'status',label:'Status',options:['available','occupied','cleaning','blocked']}],
 surgery:[{key:'patientId',label:'Patient ID'},{key:'procedure',label:'Procedure'},{key:'scheduledAt',label:'Scheduled date/time',type:'datetime-local'}],
 maternity:[{key:'patientId',label:'Patient ID'},{key:'event',label:'Maternity event'},{key:'status',label:'Status'}],
 pediatrics:[{key:'patientId',label:'Patient ID'},{key:'assessment',label:'Assessment'},{key:'weight',label:'Weight'}],
 immunization:[{key:'patientId',label:'Patient ID'},{key:'vaccine',label:'Vaccine'},{key:'dose',label:'Dose'}],
 'chronic-care':[{key:'patientId',label:'Patient ID'},{key:'condition',label:'Condition'},{key:'status',label:'Status'}],
 'care-plans':[{key:'patientId',label:'Patient ID'},{key:'encounterId',label:'Encounter ID'},{key:'problem',label:'Care problem'},{key:'goal',label:'Goal'},{key:'status',label:'Status'}],
 referrals:[{key:'patientId',label:'Patient ID'},{key:'encounterId',label:'Encounter ID'},{key:'reason',label:'Referral reason'},{key:'destination',label:'Destination'}],
 'follow-up':[{key:'patientId',label:'Patient ID'},{key:'reason',label:'Follow-up reason'},{key:'dueAt',label:'Due date/time',type:'datetime-local'}],
 billing:[{key:'patientId',label:'Patient ID'},{key:'encounterId',label:'Encounter ID'},{key:'description',label:'Charge description'},{key:'total',label:'Total'},{key:'currency',label:'Currency',options:['UGX','USD','EUR','KES','TZS']}],
 payments:[{key:'invoiceId',label:'Invoice ID'},{key:'amount',label:'Amount'},{key:'method',label:'Method',options:['cash','mobile-money','card','bank']}],
 insurance:[{key:'patientId',label:'Patient ID'},{key:'provider',label:'Provider'},{key:'policyNumber',label:'Policy number'}],
 claims:[{key:'patientId',label:'Patient ID'},{key:'claimNumber',label:'Claim number'},{key:'status',label:'Status',options:['draft','submitted','approved','rejected']}],
 accounting:[{key:'description',label:'Entry description'},{key:'amount',label:'Amount'},{key:'type',label:'Type',options:['income','expense','adjustment']}],
 inventory:[{key:'name',label:'Item name'},{key:'sku',label:'SKU'},{key:'quantity',label:'Quantity'},{key:'unit',label:'Unit'}],
 procurement:[{key:'supplier',label:'Supplier'},{key:'description',label:'Order description'},{key:'quantity',label:'Quantity'}],
 suppliers:[{key:'name',label:'Supplier name'},{key:'phone',label:'Phone'},{key:'email',label:'Email'}],
 'patient-portal':[{key:'patientId',label:'Patient ID'},{key:'feature',label:'Portal feature'},{key:'status',label:'Status'}],
 telemedicine:[{key:'patientId',label:'Patient ID'},{key:'scheduledAt',label:'Scheduled date/time',type:'datetime-local'},{key:'meetingUrl',label:'Meeting URL'}],
 'remote-monitoring':[{key:'patientId',label:'Patient ID'},{key:'device',label:'Device'},{key:'reading',label:'Reading'}],
 notifications:[{key:'patientId',label:'Patient ID'},{key:'channel',label:'Channel',options:['in-app','sms','email','whatsapp']},{key:'message',label:'Message'}],
 documents:[{key:'patientId',label:'Patient ID'},{key:'name',label:'Document name'},{key:'type',label:'Document type'}],
 ai:[{key:'patientId',label:'Patient ID'},{key:'purpose',label:'AI purpose',required:true},{key:'question',label:'Clinical question',required:true},{key:'clinicalContext',label:'Clinical context JSON'}],
 analytics:[{key:'name',label:'Metric name'},{key:'value',label:'Value'},{key:'period',label:'Period'}],
 'population-health':[{key:'name',label:'Population metric'},{key:'value',label:'Value'},{key:'period',label:'Period'}],
 staff:[{key:'name',label:'Staff name'},{key:'role',label:'Role'},{key:'phone',label:'Phone'}],
 facilities:[{key:'name',label:'Facility name'},{key:'type',label:'Facility type'},{key:'status',label:'Status'}],
 tasks:[{key:'title',label:'Task title'},{key:'patientId',label:'Patient ID'},{key:'priority',label:'Priority',options:['normal','urgent','critical']}],
 workflows:[{key:'name',label:'Workflow name'},{key:'trigger',label:'Trigger'},{key:'status',label:'Status'}],
 interoperability:[{key:'system',label:'Connected system'},{key:'standard',label:'Standard',options:['FHIR','HL7v2','DICOM','DHIS2']},{key:'status',label:'Status'}],
 trust:[{key:'control',label:'Control'},{key:'status',label:'Status'}],
 audit:[{key:'action',label:'Action'},{key:'entityType',label:'Entity type'},{key:'entityId',label:'Entity ID'}],
 settings:[{key:'name',label:'Setting name'},{key:'value',label:'Value'}],
 'command-center':[{key:'title',label:'Command item'},{key:'status',label:'Status'},{key:'priority',label:'Priority',options:['normal','urgent','critical']}]
};

export default function Home(){
 const [token,setToken]=useState(''),[module,setModule]=useState('patients'),[rows,setRows]=useState<Row[]>([]),[dash,setDash]=useState<Row>({}),[status,setStatus]=useState('Connecting'),[error,setError]=useState(''),[search,setSearch]=useState(''),[patient360,setPatient360]=useState<Row|null>(null),[encounter,setEncounter]=useState<Row|null>(null),[searchResults,setSearchResults]=useState<Row>({});
 async function login(){try{const r=await fetch(`${API}/api/auth/demo`,{method:'POST'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Login failed');setToken(d.token)}catch(e:any){setError(e.message);setStatus('Offline')}}
 async function load(){if(!token)return;setStatus('Loading');setError('');try{const endpoint=module==='laboratory'?'/api/laboratory/results':`/api/${module}`;const r=await fetch(`${API}${endpoint}`,{headers:{Authorization:`Bearer ${token}`}});const d=await r.json();if(!r.ok)throw new Error(d.error||`Unable to load ${pretty(module)}`);setRows(d.data||[]);setStatus('Ready')}catch(e:any){setRows([]);setError(e.message);setStatus('Error')}}
 async function refreshDash(){if(!token)return;try{const r=await fetch(`${API}/api/dashboard`,{headers:{Authorization:`Bearer ${token}`}});if(r.ok)setDash(await r.json())}catch{}}
 async function runSearch(){if(!token||search.trim().length<2){setSearchResults({});return}try{const r=await fetch(`${API}/api/search?q=${encodeURIComponent(search.trim())}`,{headers:{Authorization:`Bearer ${token}`}});const d=await r.json();setSearchResults(d)}catch{setSearchResults({})}}
 async function openPatient(id:string){try{const r=await fetch(`${API}/api/patients/${id}/360`,{headers:{Authorization:`Bearer ${token}`}});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to open patient');setPatient360(d)}catch(e:any){setError(e.message)}}
 useEffect(()=>{login(); if('serviceWorker' in navigator) navigator.serviceWorker.register('/sw.js').catch(()=>{})},[]);useEffect(()=>{if(token){load();refreshDash()}},[token,module]);
 const title=module==='command-center'?'Command Center':pretty(module);
 return <main>
  <aside><div className="brand"><b>ClinAI</b><span>Connected Healthcare OS</span></div>{groups.map(([g,items]:any)=><section key={g}><small>{g}</small>{items.map((x:string)=><button key={x} className={module===x?'active':''} onClick={()=>{setModule(x);setError('');setSearchResults({})}}>{pretty(x)}</button>)}</section>)}</aside>
  <div className="content">
   <header><div><small>CLINAI WORKSPACE</small><h1>{title}</h1><p>Connected care, clinical operations and intelligence in one workspace.</p></div><div className="status">● {status}</div></header>
   <div className="toolbar"><input value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>e.key==='Enter'&&runSearch()} placeholder="Search patients, encounters, appointments or orders"/><button onClick={runSearch}>Search</button></div>
   {Object.keys(searchResults).length>0&&<section className="searchpanel"><b>Search results</b><div className="searchgrid">{Object.entries(searchResults).map(([k,v]:any)=><div key={k}><small>{pretty(k)}</small>{(v||[]).map((r:Row)=><button key={r.id} onClick={()=>r.firstName&&openPatient(r.id)}>{r.firstName?`${r.firstName} ${r.lastName}`:r.display||r.reason||r.status||r.id}</button>)}</div>)}</div></section>}
   <div className="metrics">{[['Patients',dash.patients],['Appointments',dash.appointments],['Waiting',dash.waiting],['Critical labs',dash.criticalLabs],['Open tasks',dash.openTasks],['Unpaid',dash.unpaid]].map(([n,v])=><div className="metric" key={String(n)}><strong>{v??0}</strong><span>{n}</span></div>)}</div>
   {error&&<div className="error">{error}</div>}
   <div className="grid"><section className="panel"><div className="panelhead"><div><small>WORKSPACE</small><h2>{title}</h2></div><button onClick={()=>{load();refreshDash()}}>Refresh</button></div><Action module={module} token={token} done={()=>{load();refreshDash()}}/><div className="list">{rows.length?rows.map(r=><article key={r.id}><div><b>{r.firstName?`${r.firstName} ${r.lastName}`:r.medicationName||r.name||r.title||r.description||r.display||r.reason||r.task||r.code||r.template||r.status||'Record'}</b><span>{r.patientNumber||r.barcode||r.phone||r.priority||r.status||r.createdAt||r.updatedAt||''}</span></div><div className="rowright"><em>{r.status||'active'}</em><RowActions module={module} row={r} token={token} done={()=>{load();refreshDash()}} openPatient={openPatient} openEncounter={setEncounter}/></div></article>):<div className="empty">No records yet. Create the first record above.</div>}</div></section>
   <section className="panel"><div className="panelhead"><div><small>CONNECTED CARE</small><h2>Clinical journey</h2></div></div>{journey.map(([x,m]:any,i:number)=><button className="journey" key={x} onClick={()=>setModule(m)}><i>{i+1}</i><span>{x}</span><em>{module===m?'open':'connected'}</em></button>)}</section></div>
  </div>
  {patient360&&<Patient360 data={patient360} close={()=>setPatient360(null)} setModule={setModule}/>} 
 </main>
}

function Action({module,token,done}:{module:string;token:string;done:()=>void}){const [d,setD]=useState<Row>({}),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');const fs=fields[module]||[{key:'name',label:'Name'},{key:'description',label:'Description'},{key:'status',label:'Status'}];
 async function submit(){setBusy(true);setMsg('');try{let body={...d};if(['appointments','follow-up','surgery','telemedicine'].includes(module)){const key=module==='appointments'?'startAt':module==='follow-up'?'dueAt':'scheduledAt';if(body[key])body[key]=new Date(body[key]).toISOString()}if(['triage'].includes(module)){for(const k of ['temperature','heartRate','respiratoryRate','systolic','diastolic','spo2','pain'])if(body[k]!==undefined&&body[k]!=='')body[k]=Number(body[k])}if(module==='orders'){body.category=body.category||'laboratory';body.priority=body.priority||'routine'}
   let url=`${API}/api/${module}`;if(module==='laboratory'||module==='imaging')body={patientId:body.patientId,encounterId:body.encounterId,category:module==='laboratory'?'laboratory':'imaging',code:body.code||'GENERAL',description:body.description,priority:body.priority||'routine'};if(module==='laboratory'||module==='imaging')url=`${API}/api/orders`;if(module==='patients')body.email=body.email||undefined;
   const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify(body)});const out=await r.json();if(!r.ok)throw new Error(out.error||'Request failed');setD({});setMsg('Saved successfully');done()}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}
 return <div className="quick">{fs.map(f=>f.options?<select key={f.key} value={d[f.key]??''} onChange={e=>setD({...d,[f.key]:e.target.value})}><option value="">{f.label}</option>{f.options.map(o=><option key={o}>{o}</option>)}</select>:<input key={f.key} type={f.type||'text'} placeholder={f.placeholder||f.label} value={d[f.key]??''} onChange={e=>setD({...d,[f.key]:e.target.value})}/>)}<button disabled={busy||!token} onClick={submit}>{busy?'Saving…':'Create'}</button>{msg&&<span className="formmsg">{msg}</span>}</div>
}

function RowActions({module,row,token,done,openPatient,openEncounter}:{module:string;row:Row;token:string;done:()=>void;openPatient:(id:string)=>void;openEncounter:(row:Row)=>void}){const [busy,setBusy]=useState(false);const actions:string[]=[];if(module==='patients')actions.push('360');if(module==='encounters')actions.push('open');if(module==='appointments'&&!['cancelled','completed','no-show','arrived','checked-in'].includes(row.status))actions.push('check-in');if(module==='queue'&&!['completed','cancelled','no-show'].includes(row.status))actions.push(row.status==='waiting'||row.status==='waiting-triage'?'called':row.status==='called'?'in-service':'completed');if(module==='clinical-notes'&&!row.signedAt)actions.push('sign');if(module==='laboratory')actions.push(row.status==='preliminary'?'verify':row.status==='verified'?'release':'');if(module==='pharmacy'&&['active','partially-dispensed'].includes(row.status))actions.push('dispense');if(module==='billing'&&row.status!=='paid')actions.push('pay');if(module==='tasks'&&row.status!=='completed')actions.push('complete');if(module==='referrals'&&row.status==='draft')actions.push('send');if(module==='inpatient')actions.push('discharge');
 const run=async(a:string)=>{if(!a)return;setBusy(true);try{if(a==='360'){openPatient(row.id);return}if(a==='open'){openEncounter(row);return}let url=`${API}/api/actions/${module}/${row.id}/${a}`;let body:any={};if(a==='pay')body={amount:Number(row.total||0)-Number(row.paidAmount||0),method:'cash'};if(a==='dispense')body={quantity:Number(row.quantity||1)};const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error||'Action failed');done()}catch(e:any){alert(e.message)}finally{setBusy(false)}};
 return <div className="rowactions">{actions.filter(Boolean).map(a=><button key={a} disabled={busy} onClick={()=>run(a)}>{a==='check-in'?'Check in':a==='360'?'Patient 360':a==='open'?'Open workspace':a==='sign'?'Sign':a==='verify'?'Verify':a==='release'?'Release':a==='dispense'?'Dispense':a==='pay'?'Pay':a==='complete'?'Complete':a==='send'?'Send':a==='called'?'Call':a==='in-service'?'Start service':a==='completed'?'Complete':'Discharge'}</button>)}</div>
}


function EncounterWorkspace({data,token,close,done}:{data:Row;token:string;close:()=>void;done:()=>void}){
 const [tab,setTab]=useState('SOAP'),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false); const [soap,setSoap]=useState<Row>({noteType:'SOAP'}); const [dx,setDx]=useState<Row>({}); const [order,setOrder]=useState<Row>({category:'laboratory',priority:'routine'});
 async function post(url:string,body:Row,success:string){setBusy(true);setMsg('');try{const r=await fetch(`${API}${url}`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');setMsg(success);done();return d}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}
 return <div className="modalback" onClick={close}><section className="modal" onClick={e=>e.stopPropagation()}><div className="panelhead"><div><small>CLINICAL ENCOUNTER</small><h2>Encounter workspace</h2><span>{data.id} · Patient {data.patientId} · {data.status}</span></div><button onClick={close}>Close</button></div><div className="tabs"><button className={tab==='SOAP'?'tabactive':''} onClick={()=>setTab('SOAP')}>SOAP note</button><button className={tab==='DIAGNOSIS'?'tabactive':''} onClick={()=>setTab('DIAGNOSIS')}>Diagnosis</button><button className={tab==='ORDER'?'tabactive':''} onClick={()=>setTab('ORDER')}>Order</button></div>{tab==='SOAP'&&<div className="workspaceform"><textarea placeholder="Subjective" value={soap.subjective||''} onChange={e=>setSoap({...soap,subjective:e.target.value})}/><textarea placeholder="Objective" value={soap.objective||''} onChange={e=>setSoap({...soap,objective:e.target.value})}/><textarea placeholder="Assessment" value={soap.assessment||''} onChange={e=>setSoap({...soap,assessment:e.target.value})}/><textarea placeholder="Plan" value={soap.plan||''} onChange={e=>setSoap({...soap,plan:e.target.value})}/><button disabled={busy} onClick={()=>post('/api/clinical-notes',{...soap,encounterId:data.id},'SOAP note saved')}>Save note</button></div>}{tab==='DIAGNOSIS'&&<div className="workspaceform"><input placeholder="ICD-10 code" value={dx.code||''} onChange={e=>setDx({...dx,code:e.target.value})}/><input placeholder="Diagnosis description" value={dx.description||''} onChange={e=>setDx({...dx,description:e.target.value})}/><button disabled={busy} onClick={()=>post('/api/diagnoses',{...dx,patientId:data.patientId,encounterId:data.id},'Diagnosis added')}>Add diagnosis</button></div>}{tab==='ORDER'&&<div className="workspaceform"><select value={order.category||'laboratory'} onChange={e=>setOrder({...order,category:e.target.value})}><option value="laboratory">Laboratory</option><option value="imaging">Imaging</option><option value="medication">Medication</option><option value="procedure">Procedure</option></select><input placeholder="Order code" value={order.code||''} onChange={e=>setOrder({...order,code:e.target.value})}/><input placeholder="Description" value={order.description||''} onChange={e=>setOrder({...order,description:e.target.value})}/><select value={order.priority||'routine'} onChange={e=>setOrder({...order,priority:e.target.value})}><option>routine</option><option>urgent</option><option>stat</option></select><button disabled={busy} onClick={()=>post('/api/orders',{...order,patientId:data.patientId,encounterId:data.id},'Order placed')}>Place order</button></div>}{msg&&<div className="formmsg workspaceMsg">{msg}</div>}<div className="encounterhint">Clinical AI can assist with documentation and review, but clinician judgment remains required before diagnosis, treatment or release decisions.</div></section></div>
}

function Patient360({data,close,setModule}:{data:Row;close:()=>void;setModule:(m:string)=>void}){const p=data.patient;return <div className="modalback" onClick={close}><section className="modal" onClick={e=>e.stopPropagation()}><div className="panelhead"><div><small>PATIENT 360</small><h2>{p.firstName} {p.lastName}</h2><span>{p.patientNumber} · {p.status}</span></div><button onClick={close}>Close</button></div><div className="patientgrid"><div><b>Identity</b><p>DOB: {p.dateOfBirth||'Not recorded'}</p><p>Sex: {p.sex||'Not recorded'}</p><p>Phone: {p.phone||'Not recorded'}</p><p>Email: {p.email||'Not recorded'}</p></div><div><b>Safety</b><p>Allergies: {data.allergies?.length||0}</p>{data.allergies?.slice(0,4).map((x:Row)=><p key={x.id}>{x.substance} · {x.severity||'unspecified'}</p>)}</div><div><b>Active care</b><p>Diagnoses: {data.diagnoses?.length||0}</p><p>Orders: {data.orders?.length||0}</p><p>Clinical notes: {data.clinicalNotes?.length||0}</p><p>Appointments: {data.appointments?.length||0}</p></div><div><b>Notifications</b><p>{data.notifications?.length||0} active items</p>{data.notifications?.slice(0,4).map((x:Row)=><p key={x.id}>{x.template} · {x.status}</p>)}</div></div><div className="modalnav"><button onClick={()=>{setModule('encounters');close()}}>Encounters</button><button onClick={()=>{setModule('orders');close()}}>Orders</button><button onClick={()=>{setModule('pharmacy');close()}}>Pharmacy</button><button onClick={()=>{setModule('billing');close()}}>Billing</button><button onClick={()=>{setModule('follow-up');close()}}>Follow-up</button></div></section></div>}
