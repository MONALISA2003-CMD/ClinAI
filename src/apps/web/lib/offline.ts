export type OfflineOperation={id:string;url:string;method:string;body:any;createdAt:string;retries:number}
const DB='clinai-offline-v1', STORE='operations'
function openDb():Promise<IDBDatabase>{return new Promise((resolve,reject)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>r.result.createObjectStore(STORE,{keyPath:'id'});r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
export async function enqueueOperation(op:Omit<OfflineOperation,'id'|'createdAt'|'retries'>){if(typeof indexedDB==='undefined')return false;const db=await openDb();const value:OfflineOperation={...op,id:crypto.randomUUID(),createdAt:new Date().toISOString(),retries:0};await new Promise<void>((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).put(value);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)});return true}
export async function pendingOperations():Promise<OfflineOperation[]>{if(typeof indexedDB==='undefined')return [];const db=await openDb();return new Promise((resolve,reject)=>{const tx=db.transaction(STORE,'readonly');const r=tx.objectStore(STORE).getAll();r.onsuccess=()=>resolve(r.result||[]);r.onerror=()=>reject(r.error)})}
export async function flushOperations(token:string){if(!navigator.onLine)return 0;const ops=await pendingOperations();let done=0;for(const op of ops){try{const r=await fetch(op.url,{method:op.method,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`,'Idempotency-Key':op.id},body:JSON.stringify(op.body)});if(!r.ok)continue;const db=await openDb();await new Promise<void>((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).delete(op.id);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)});done++}catch{break}}return done}
